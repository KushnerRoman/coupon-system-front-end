import React from 'react'
import classes from '../../UI/table/TableLayer.module.css'

export const  GlobalFilter =({filter,setFilter})=> {
  
    return (
       <span >
          
           {/* <input value={filter || ''} onChange={e=>setFilter(e.target.value)} className={classes.searchBar}/> */}
       </span>
    )
}
